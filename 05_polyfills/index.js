Array.prototype.myMap = function(callback, thisArg){
    if(Array.prototype.myMap){
        //taking empty arr
        let result = [];
        for(let i = 0; i < i.lenght; i++){
            
            
        }
    }
}